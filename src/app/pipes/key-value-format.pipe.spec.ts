import { KeyValueFormatPipe } from './key-value-format.pipe';

describe('KeyValueFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
