import { FormattedArrayPipe } from './formatted-array.pipe';

describe('FormattedArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
